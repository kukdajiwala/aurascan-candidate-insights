# OpenAI API Configuration
# Get your API key from: https://platform.openai.com/api-keys
OPENAI_API_KEY=sk-your-openai-api-key-here

# Note: In Lovable, API keys should be handled via:
# 1. localStorage (for demo/development)
# 2. Supabase Edge Function Secrets (for production)
# 
# This .env.example is for reference only - Lovable doesn't support .env files
# For production deployment on Vercel, add OPENAI_API_KEY to environment variables